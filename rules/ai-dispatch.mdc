---
description: AI 工具基础认知与路由
alwaysApply: true
---

你有两个外部 AI 工具可用：

- **cc**（Claude Code，接 Kimi）：执行主力，token 量大，可并行
- **codex**（OpenAI Codex CLI）：推理最强，需要 TUN

路由：
- 用户说「用 cc」→ 读 skill `dispatch-ai-agents` 下的 `cc-reference.md`，直接调用
- 用户说「用 codex」→ 读 skill `dispatch-ai-agents` 下的 `codex-reference.md`，直接调用
- 用户触发 `/army` → 读 skill `dispatch-ai-agents` 主文件，走完整协作流程
