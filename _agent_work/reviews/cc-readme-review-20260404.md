# README.md 改进建议

## 1. 补充「批量操作」判定标准到铁律

**问题**：根据提交历史 "README 补铁律（≥3文件视为批量、执行后写日志）"，但当前铁律部分缺少"≥3文件视为批量"的明确规则。

**建议**：在铁律部分添加：
```
- 单次操作 ≥3 个文件视为批量任务，必须走 /army 流程
```

---

## 2. 安装部分缺少前置条件说明

**问题**：安装命令 `bash install.sh` 过于简单，缺少环境依赖说明（claude CLI、codex CLI 安装、CC_PATH 环境变量配置等）。

**建议**：在安装章节添加前置条件清单：
- 需先安装 claude CLI 并配置 KIMI_API_KEY
- 需安装 codex CLI 并配置 OPENAI_API_KEY
- 需设置 CC_PATH 环境变量
- 提供安装验证命令

---

## 3. 增加「快速开始」完整示例

**问题**：虽然有命令速查，但缺少一个完整的端到端示例，新用户难以快速理解整个工作流。

**建议**：在文档末尾添加「快速开始」章节，展示一个完整场景：
1. 用户输入任务
2. 调用 `/army` 生成分工计划
3. 调用 cc 执行分析
4. 调用 codex 执行代码生成
5. 验收 review 结果

---

*Review 日期：2026-04-04*
